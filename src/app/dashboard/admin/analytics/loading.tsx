import { PageLoading } from "@/shared/components/loading";

export default function AnalyticsLoadingPage() {
  return (
    <PageLoading
      title="Loading Analytics"
      subtitle="Please wait while we prepare your analytics dashboard..."
    />
  );
}
